import { defineConfig } from 'vite';

export default defineConfig({
  // Caminhos relativos funcionam tanto em domínios próprios quanto em
  // projetos publicados em https://usuario.github.io/repositorio/.
  base: './',
});
